// Revisão de funções

function exemplo01(name) {
  return name.toUpperCase()
}

const exemplo02 = function(name) {
  return name.toUpperCase()
}

/*------------------------- ARROW FUNCTIONS ---------------------------- */
const exemplo03 = name => {
  return name.toUpperCase()
}

// Quando só tem uma linha no return vc pode trar as {} e o return

const exemplo04 = name => name.toUpperCase()

